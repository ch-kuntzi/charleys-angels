# Task Dashboard

A simple task dashboard application built with React and Vite.

## Status

The application has been repaired and now adheres to the standard branding guidelines. The build process is successful.

## Development

To run the application in development mode:

```bash
npm run dev
```

## Build

To build the application for production:

```bash
npm run build
```
